name: Static site CI

on:
  pull_request:
    branches: [ "main" ]

jobs:
  validate-site-path:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: Ensure web directory exists
        run: test -d web
